# Point the frontend at the Python (FastAPI) backend.
# The frontend automatically prepends /api/v1 to all requests.
# Leave blank to use the bundled mock data instead.
VITE_API_BASE_URL=http://localhost:8000
VITE_REALTIME_WS_URL=ws://localhost:8000/api/v1/ws/realtime
